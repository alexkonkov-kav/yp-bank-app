server:
  port: 8084

spring:
  application:
    name: transfer-app
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/bank_db}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:qwerty}

  liquibase:
    enabled: true
    change-log: classpath:db/master.xml
    default-schema: ${TRANSFER_DB_SCHEMA:transfer_schema}

  jpa:
    properties:
      hibernate:
        default_schema: ${TRANSFER_DB_SCHEMA:transfer_schema}
    show-sql: true
    hibernate:
      ddl-auto: none

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI:http://localhost:8080/realms/bank-realm}
          #issuer-uri: http://host.docker.internal:8080/realms/bank-realm
      client:
        registration:
          transfer-app:
            provider: keycloak
            client-id: transfer-app
            client-secret: transfer-app-secret
            authorization-grant-type: client_credentials
        provider:
          keycloak:
            issuer-uri: ${SPRING_SECURITY_OAUTH2_CLIENT_PROVIDER_KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/bank-realm}
            #issuer-uri: http://host.docker.internal:8080/realms/bank-realm
  kafka:
    bootstrap-servers: kafka-broker-0.kafka-headless.default.svc.cluster.local:9092
    #bootstrap-servers: localhost:9093
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 3

bank:
  account-gateway:
    base-url: http://localhost:8086/accounts

management:
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  level:
    root: INFO